# 售前项目管道管理平台 · 最终方案

> 版本：v2.0（按"前端 / 其他"重构）　日期：2026-08-17
> 状态：已确认需求，待开工

---

## 0. 项目背景（共同部分）

售前人员目前用个人 Excel 维护项目管道，再手工汇总进领导的总表；数据需从源平台导出后人工转换，两张表反复誊写易错乱。本平台的目标：**用一个自部署的在线平台替代"双表维护"——平台是唯一数据源，Excel 导入导出只是进出通道，导出仅是快照。**

**使用者与规模**：现阶段单人使用，项目量级几百条；架构为未来多人共用预留余地。

**整体架构一句话**：浏览器里的 React 单页应用（前端）通过 REST API 与 Spring Boot 后端通信，后端将数据持久化到 MySQL；Excel 的解析与生成全部在前端完成，文件不经过服务器。

下文分两条线展开：**一、前端**；**二、其他**（后端、数据库、部署运维、非功能）。

---

# 一、前端

## 1.1 前端 · 目标定义

1. **一张可维护的管道总表**：项目列表支持行内编辑、筛选（行业/赛道/关键字）、列显隐与排序配置，收入列实时显示按月汇总值。
2. **项目全景详情**：单个项目一处看全——基础字段、进展时间线、按月收入、修改日志。
3. **进展可记可溯**：进展按"日期 + 内容"逐条追加，时间线倒序展示，历史完整回溯。
4. **收入按月可填可统计**：矩阵式录入界面（项目为行、月份为列，批量保存）；统计界面支持按项目、行业、月度、年度切换维度，图表 + 汇总表呈现。
5. **Excel 导入向导化**：上传 → 映射调整 → 确认执行，三步完成；能处理"源表列多于所需、列名不同、一列拆多字段"的情况；导入前明确告知"新增 X / 覆盖 Y / 跳过 Z"。
6. **Excel 导出拿来即用**：按可配置模板（列名、列顺序完全自定义）导出，直接粘贴/提交领导总表，无需再调格式；支持导出主表、收入明细、进展日志。
7. **配置自助化**：字典（赛道/行业/子行业/解决方案，行业→子行业级联）、导入映射方案、导出模板、自定义扩展字段，全部在界面上自行增删改，无需改代码。

## 1.2 前端 · 方案

### 技术栈

React 19 + TypeScript + Vite + Tailwind CSS + shadcn/ui；数据请求 TanStack Query；Excel 解析与生成用 SheetJS（xlsx）；统计图用 ECharts。构建产物为纯静态文件，由后端 jar 托管（也可挂 nginx）。

### 页面结构（6 个页面）

| 页面 | 路由 | 内容 |
|---|---|---|
| 项目管道列表 | `/` | 全字段表格（收入列实时汇总）、行内编辑、筛选、列配置；新建/导入/导出入口 |
| 项目详情抽屉 | 列表内弹出 | 基础字段编辑（行业→子行业级联下拉）；进展时间线（顶部"日期+内容"录入框，倒序展示）；按月收入录入小矩阵；修改日志页签 |
| 收入统计 | `/revenue` | 录入模式：项目×月份矩阵批量保存；统计模式：维度切换（项目/行业/月度/年度）+ 柱状/饼图 + 汇总表 |
| 导入向导 | `/import` | 三步：①上传 xlsx 本地解析预览前 20 行 → ②列名映射 + 值映射调整（可另存为方案）→ ③确认页显示"新增/覆盖/跳过"数量，执行后展示明细 |
| 导出 | `/export` | 选模板或临时选列/拖拽调序 → 预览 → 下载；范围含主表/收入明细/进展日志；可另存为模板 |
| 配置 | `/settings` | 字典树管理（增删改排序、删除保护提示）；导入映射方案管理；导出模板管理；自定义字段定义（文本/数值/日期/选项四类） |

### 前端承担的核心逻辑

- **Excel 解析（导入）**：SheetJS 读取 xlsx → 按列名映射抽取字段（未映射列忽略）→ 按值映射规则转换 → 进展单元格按换行符拆成多条时间线记录（格式约定 `日期：内容`，无日期的归入导入当天）→ 组装 JSON 提交后端。
- **值映射三种规则（界面可配，可组合）**：精确对照表（如 `金融行业 → 行业=金融、子行业=银行`，实现一列拆多字段）、分隔符拆分（如 `金融-银行` 按 `-` 拆）、固定默认值（源表缺失字段补常量）。
- **Excel 生成（导出）**：从后端取 JSON → 按模板列名/列序生成 xlsx 下载。
- **校验**：金额数值、月份 `YYYY-MM`、必填字段，前端先校验再提交。
- **自定义字段全链路生效**：字段定义后自动出现在详情表单、列表可选列、导入映射目标、导出可选列中。

---

# 二、其他（后端 / 数据库 / 部署运维 / 非功能）

## 2.1 其他 · 目标定义

1. **统一持久化**：所有业务数据存 MySQL，平台是唯一数据源，替代"双表维护"。
2. **判重与可追溯的导入**：导入按 `external_id`（外部系统编号，可选）优先、其次"客户名称+项目名称"判重；同项目覆盖更新；覆盖与手工修改均留下字段级修改日志（旧值→新值）。
3. **收入模型正确**：收入按"项目×月份"存储（唯一约束，重复录入即覆盖）；项目"收入"由后端实时汇总，主表不落库。
4. **统计在服务端聚合**：按项目/行业/月度/年度的收入统计在 SQL 层 group by 完成，前端只拿结果。
5. **可扩展**：自定义字段以 JSON 列 + 字段定义配置承载，加字段不改表结构；字典、映射、模板全部数据驱动。
6. **数据安全兜底**：软删除可恢复；每日自动备份保留 30 天；支持一键全量导出。
7. **可部署、可运行**：交付完整代码 + docker-compose，一台有 Docker 的内网主机即可一键拉起；前后端分离但部署一体化（后端托管前端静态文件）。
8. **无登录但可演进**：现阶段不做鉴权，API 预留"操作人"概念，未来加 Spring Security / SSO 业务代码零改动。

## 2.2 其他 · 方案

### 后端

**技术栈**：Java 17 + Spring Boot 3 + Spring Data JPA + Maven + MySQL 8；Bean Validation 参数校验；统一响应结构 `{code, message, data}`；全局异常处理。

**API 分组（统一前缀 /api）**：

| 分组 | 端点（摘选） | 说明 |
|---|---|---|
| /api/projects | GET 列表（行业/赛道/关键字/月份区间筛选，分页）· POST · PUT · DELETE（软删） | 列表联查收入汇总；写操作记 change_logs |
| /api/projects/{id}/detail | GET | 项目 + 进展时间线 + 按月收入 + 修改日志，一次取全 |
| /api/revenues | GET 矩阵 · PUT 批量保存 | 按 `(project_id, month)` upsert，覆盖写日志 |
| /api/stats/revenue | GET ?dim=project/industry/month/year&year=… | SQL 层聚合 |
| /api/progress | GET / POST / DELETE | 时间线增删查 |
| /api/dictionaries | GET 树（?type=）· CRUD | 级联查询；被引用项禁止删除并提示 |
| /api/import | POST（已解析 JSON + 映射方案 ID） | 判重 → 新增/覆盖（逐字段 diff 写日志）→ 返回结果明细 |
| /api/export | POST {templateId 或临时列配置} | 返回 JSON 供前端生成 xlsx |
| /api/config | 映射方案 / 导出模板 / 自定义字段定义 CRUD | |
| /api/backup | GET 全量导出（JSON） | 一键备份双保险 |

**无登录的处理**：不挂鉴权；`change_logs.operator` 现阶段固定写"我"，为未来用户体系预留。

### 数据库（8 张表）

> 约定：全表含 `id`（BIGINT 自增）、`created_at`、`updated_at`；业务表含 `deleted` 软删除标记；金额 `DECIMAL(18,2)` 单位万元；月份 `CHAR(7)` 格式 `YYYY-MM`。

| 表 | 关键字段与约束 |
|---|---|
| projects | external_id（可空，唯一索引，判重首选）、customer_name、project_name、safety_space、solution、track、industry、sub_industry、scenario、key_risks、key_needs、custom_fields(JSON)；(customer_name, project_name, deleted) 联合索引 |
| revenues | project_id、month、amount；唯一约束 (project_id, month) |
| progress_logs | project_id、log_date、content；(project_id, log_date desc) 索引 |
| dictionaries | type（track/industry/solution…）、value、parent_id（自引用实现行业→子行业级联）、sort |
| import_mappings | name、column_map(JSON 列名映射)、value_rules(JSON 三种规则) |
| export_templates | name、scope（projects/revenues/progress）、columns(JSON 有序列配置) |
| change_logs | project_id、operator、field、old_value、new_value、source（manual/import） |
| 说明 | 无 users 表（不做登录），operator 字段预留 |

### 部署运维

- **docker-compose 一键编排**：MySQL 8（utf8mb4、Asia/Shanghai、数据卷持久化）+ 后端 jar（托管前端静态文件）+ 备份容器（每日 mysqldump，保留 30 天）；健康检查 + `restart: unless-stopped`。
- **非 Docker 备选**：JDK 17 + MySQL 8，改 `application.yml` 数据源后 `java -jar`。
- **访问控制（部署方配合）**：无登录意味着能访问地址即可读写，**务必**部署于内网或配置 nginx IP 白名单（README 附示例）。

### 交付物

```
presales-pipeline/
├── frontend/          # React SPA 源码
├── backend/           # Spring Boot 源码（Maven）
├── docker-compose.yml # MySQL + 后端 + 定时备份
├── db/init.sql        # 建库与字符集初始化
├── scripts/demo-data.sql + 示例导入.xlsx   # 演示数据（可选灌入）
└── README.md          # 部署文档：环境、启动、备份恢复、白名单示例
```

---

## 三、实施顺序与已知边界（共同部分）

**实施顺序**：① 数据模型与前后端工程骨架（含 compose）→ ② 字典/配置模块（被其他模块依赖）→ ③ 项目主表 CRUD + 自定义字段 → ④ 进展时间线 + 收入矩阵 → ⑤ 统计 → ⑥ 导入向导 → ⑦ 导出 → ⑧ 备份、演示数据、README、联调打包。

**已知边界**：
- 本平台无法运行 Java，后端不做在线预览；交付后在你环境联调，问题随时回来改。
- 多人协作、权限体系、审批流为二期预留，不在本期范围。
